{
  "name": "ml-brokerage",
  "version": "1.0.0",
  "description": "",
  "main": "src/server/mlbrokerage.entrypoint.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "nodemon",
    "start": "node",
    "migration:generate": "npx sequelize-cli migration:generate --name",
    "migrate:up": "npx sequelize-cli db:migrate",
    "migrate:undo": "npx sequelize-cli db:migrate:undo",
    "migrate:undo:all": "npx sequelize-cli db:migrate:undo:all",
    "migrate:undo:to": "npx sequelize-cli db:migrate:undo:all --to",
    "seed:generate": "npx sequelize-cli seed:generate --name",
    "seed:all": "npx sequelize-cli db:seed:all",
    "seed:one": "npx sequelize-cli db:seed --seed",
    "seed:undo": "npx sequelize-cli db:seed:undo",
    "seed:undo:one": "npx sequelize-cli db:seed:undo --seed",
    "seed:undo:all": "npx sequelize-cli db:seed:undo:all"
  },
  "author": "M Lhuillier Financial Services Inc.",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2",
    "bcrypt": "^5.1.1",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dayjs": "^1.11.11",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "googleapis": "^140.0.1",
    "helmet": "^7.1.0",
    "joi": "^17.13.1",
    "jsonwebtoken": "^9.0.2",
    "log4js": "^6.9.1",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.10.0",
    "randomstring": "^1.3.0",
    "redis": "^4.6.15",
    "sequelize": "^6.37.3",
    "winston": "^3.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.2",
    "npm-run-all": "^4.1.5",
    "sequelize-cli": "^6.6.2"
  }
}
